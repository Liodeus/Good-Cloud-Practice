In priority :
	do the mitigations, whose missings parts
	comment the functions missings one
	add a new section to the report, warning which is something which must be review 
	bug with fold/unfold, search how to resolve it
	make an .exe ?
	csv format of the results (--csv) ?
	store data in a database ? To make a graphical evolution of each scan ?
	might be able to do thing easier with --format="value(name.basename(),logConfig.basename())"
